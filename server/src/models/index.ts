// Filepath: server/src/models/index.ts

// This file serves as the entry point for the Mongoose models.
// It imports and exports the User model, allowing it to be easily accessed throughout the application.

import User from './User.js'; // Import the User model.

// Export the User model as the default export.
export default { User };
